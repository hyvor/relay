framework:
    messenger:
        transports:
            default:
                dsn: doctrine://default

when@test:
    framework:
        messenger:
            transports:
                email: 'test://'

