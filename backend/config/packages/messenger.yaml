framework:
    messenger:
        transports:
            sync: 'sync://'
            default: 'doctrine://default'

when@test:
    framework:
        messenger:
            transports:
                default: 'test://'
                scheduler_global: 'test://'
                scheduler_server: 'test://'

